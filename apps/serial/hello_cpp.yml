version: "1.0"
buildspecs:
  cplusplus_hello:
    executor: local.bash
    type: compiler
    description: hello world program in C++ compiled with gnu compiler
    tags: [compile]
    build:
      name: gnu
      source: src/hello.cpp
    module:
      - "module load gcc"
