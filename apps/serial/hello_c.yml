version: "1.0"
buildspecs:
  c_hello:
    type: compiler
    executor: local.bash
    description: hello world program in C compiled with gnu compiler
    tags: [compile]
    source: src/hello.f90
    compilers:
      name: ["^(intel)"]
      source: src/hello.f90
    module:
      - "module load gcc"

