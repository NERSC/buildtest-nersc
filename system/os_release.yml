version: "1.0"
buildspecs:
  os_release:
    type: script
    executor: local.bash
    run: lsb_release -d
    tags: ["system"]
    status:
      regex:
        exp: "SUSE Linux Enterprise Server 15"
        stream: stdout

