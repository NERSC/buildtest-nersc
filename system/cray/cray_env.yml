version: "1.0"
buildspecs:
  cray_check:
    executor: local.python
    type: script
    shell: python
    run: |
      import os
      assert os.getenv("CRAYPE_NETWORK_TARGET") == "aries"
      assert os.getenv("CRAYPE_VERSION") == "2.6.2"
      assert os.getenv("CRAYPE_CPU_TARGET") == "haswell"
