version: "1.0"
buildspecs:
  cray_check:
    executor: local.bash
    type: script
    shell: python
    run: |
      import os
      assert os.getenv("CRAYPE_NETWORK_TARGET") == "aries"
      assert os.getenv("CRAYPE_VERSION") == "2.6.2"
